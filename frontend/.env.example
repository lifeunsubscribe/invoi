# API Configuration
# This variable is automatically injected by SST during deployment and development
# When running `npx sst dev`, the API URL is provided automatically
# For standalone frontend development without SST, set this to your API endpoint
VITE_API_URL=https://your-api-gateway-url.execute-api.region.amazonaws.com
